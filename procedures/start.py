from patrol import patrol
from grid import Grid

grid = Grid()
patrol(grid) #Ideally only patrol at certain times

print ("PATROL COMPLETE")
#Modify cron to add patrol times
# OR keep within python process
